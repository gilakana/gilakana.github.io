Menu navigation: 
change menu to a navigation bar. Add a thin horizontal navigation bar at the very top of the page in the same colour as the website background (#010038). The bar should be compact and should not be excessively tall, with its height determined by the size of the navigation text and comfortable vertical spacing around it.  The labels of the tool bar should be positioned on the right
The tool bar should have the links ‘About’ and ‘Recent Works’.The link corresponding to the page the user is currently viewing should be visually distinguished from the other navigation link. The distinction should be subtle and consistent with the website's colour palette. The two navigation links should have comfortable spacing between them and should be positioned towards the right side of the navigation bar. 
Font and colour of tool bar options:  DM Sans Regular 400 size 18 colour #dcdcdc 
The navigation bar should remain fixed at the top of the viewport while the user scrolls. 
On all pages, the signature section should remain directly below the fixed navigation bar and should never overlap it. 

Signature image and white background:
The white background of the signature section should be reduced in size in the Recent Works page and pages of individual works to match the vertical height of the blue strip of accommodating the tool bar. The signature image should be reduced in size proportionally to the white background.
On the mobile About page, the initial signature section should occupy the full height of the visible screen below the navigation bar. The two parts of the signature, ‘Gila’ and ‘Kana’, should be vertically and/or visually arranged as separate elements in the centre of the white section. I will provide the two separate images for this.
The signature section should behave as a sticky header below the navigation bar. At the top of the ‘About’ page, it should occupy the specified larger height. As the user scrolls down, the signature section should smoothly reduce in height until it reaches the smaller fixed height used on the Recent Works and individual artwork pages. Once it reaches this smaller height, it should remain at that size while the user continues scrolling. 
On the About page, as the user scrolls down, the signature should smoothly transition from its large centred position into a smaller signature positioned towards the top-left of the sticky header. The transition should be subtle and smooth and should not interfere with scrolling performance. 
In Recent Works and individual artwork pages, the signature should be on the left rather than at the centre.
The signature section should remain visible while scrolling on all pages, but on Recent Works and individual artwork pages it should remain at the smaller header height.
On the desktop About page, the initial signature section should be large enough that the headings ‘Painting with flowers.’ and ‘A ritual of remembrance.’ are fully visible without scrolling, while the first line of the biography remains just below the initial viewport and requires a small amount of scrolling to reveal. 
On mobile, the large initial signature section  on ‘About’ page should occupy the full visible viewport below the navigation bar. The page must remain vertically scrollable. The artist statement should continue naturally below the signature section rather than being contained within the signature section. As the user scrolls, the signature section should smoothly shrink into the smaller sticky header described above, allowing the artist statement to become fully visible. 
On the About page, the smaller signature/header state reached after scrolling should match the size, height and position used for the signature section on the Recent Works and individual artwork pages. 

Recent works page: 
The Recent Works page should be the default landing page when a visitor enters the website. 
There should also be a sorting option in the recent works page on the white background, to the right of the signature with the text ‘Sort by’ and a small arrow pointing down and once a user clicks it two options: ‘Order’ and below it ‘Collections’. ‘Order’ should provide the options ‘newest to oldest’ and ‘oldest to newest’. ‘Collections’ should provide the option ‘Divine Couple’, which will only have two out of all the current paintings: ‘Divine Couple: Conception - A Place Where Dreams Are Born’ and ‘Divine Couple: Crossing - The Path Unfolds’. Selecting a collection should filter the artwork grid to display only artworks belonging to that collection. There should also be an option to return to viewing ‘All works’. The system should support additional collections being added in the future without requiring a redesign. 
The filter should remain visible to the users regardless of scrolling. Selecting a sorting option should immediately reorder the artwork grid without taking the user to another page or requiring a full page reload. When the Recent Works page is first opened, artworks should be displayed in newest-to-oldest order by default. Font and text size for sorting option text should match the navigation bar and colours should match the colour scheme of the website. 
Each artwork should have a chronological position/order in addition to its publicly displayed year. The ‘Newest to oldest’ and ‘Oldest to newest’ sorting options should use this chronological order rather than the year alone. This is necessary because multiple artworks may have been created in the same year but at different points during that year. I will provide the correct chronological order for each artwork. The exact chronological position does not need to be displayed publicly. Chronological order newest to oldest: ‘El Encuentro’, ‘107 Butterfly Effect’, ‘Divine Couple: Crossing - The Path Unfolds’, ‘Divine Couple: Conception - A Place Where Dreams Are Born’ 
Change the size of the artworks so that the vertical height of all artworks is the same (change margins if necessary). Display all artwork images at a consistent visual height while preserving each artwork's original aspect ratio. Do not crop, stretch or distort the artworks. Adjust the horizontal dimensions/margins as necessary to accommodate different aspect ratios. 

Individual works pages:
On desktop individual artwork pages, reduce the horizontal outer margins compared with the current version so that the artwork image can occupy more horizontal space (without any change or stretch to its proportions). The artwork description column should retain its current width, text size, and spacing. The description should therefore remain visually unchanged while the artwork image becomes larger.  Maintain a comfortable minimum horizontal margin between the artwork image and the edge of the viewport. Do not allow the artwork image to touch or sit too close to the edge of the screen. On mobile version the current layout should remain unchanged. 
At the bottom of every individual artwork page, below the artwork description and other artwork information, include a section titled ‘Explore other works’. Below this heading, display a horizontal carousel of other artworks from the portfolio. The artwork currently being viewed should never appear in the carousel. 
The carousel should automatically draw from the full collection of artworks, so that the system continues to work as the portfolio grows. The order of artworks in the ‘Explore other works’ carousel should follow the sorting order currently selected by the user on the Recent Works page. If the user has not selected a different sorting option, the default order should be newest to oldest. The carousel should use the selected chronological order, with the currently viewed artwork removed from the sequence. 
The selected sorting preference should remain active as the user navigates between individual artwork pages. The selected sorting preference should persist for the duration of the user's current browsing session, including when navigating between the Recent Works page and individual artwork pages. If the user enters an individual artwork page directly without previously selecting a sorting preference, the carousel should use the default newest to oldest order. 
On desktop, display three artworks at a time. The artworks should be approximately the same visual height as the artwork images on the Recent Works page, while preserving the original aspect ratio of each artwork. They should therefore have a consistent visual height without being cropped or distorted. 
The carousel should use the same general image presentation as the Recent Works page, including the artwork title beneath each image. When the user hovers over an artwork in the carousel, use the same partially transparent overlay box and ‘About the work: …Read more’ treatment as on the Recent Works page, but in a smaller scale appropriate to the carousel. The overlay should remain readable and should not contain more text than can comfortably fit. 
Clicking on an artwork in the carousel should take the user to that artwork's individual page. 
Place a left arrow and right arrow on either side of the carousel to allow the user to browse through the collection.The arrows should be  semi-transparent and use the same colour as the main paragraph text (#DCDCDC). They should be thin and understated rather than visually dominant, so that they complement the artwork rather than compete with it. The arrows should be sufficiently large and clearly identifiable to be comfortable to click on desktop and tap on mobile. Clicking the right arrow should move the carousel forward to reveal the next artwork(s) in the chronological collection. Clicking the left arrow should move it backwards. The carousel should move smoothly when an arrow is clicked rather than changing abruptly. 
The carousel should loop continuously. When the user reaches the final artwork, clicking the right arrow should continue from the beginning of the collection, and vice versa. Users will be able to browse the artwors without reaching a dead end. 
All navigation links, sorting controls and carousel arrows should be keyboard accessible and have clear accessible labels. The carousel should not rely solely on hover to reveal information or navigate. 
On mobile devices, the carousel should adapt to the smaller screen. Display fewer artworks at once, ideally one artwork at a time, while maintaining the same visual style. Users should be able to navigate using the arrows and swipe horizontally through the artworks.The carousel should remain responsive as more artworks are added to the portfolio.
When a user selects an artwork from the carousel, the new individual artwork page should open at the top of that artwork page, with the sticky navigation and signature header in their normal positions. 

Font:
The “CONNECT” heading and “Recent Works” heading should use DM Sans Bold 700, size 27. 

Text:
Divine Couple: Conception - A Place Where Dreams Are Born remove ‘Set at dawn, yet shining with bright colours, I like to think of it as an evening painting’
Divine Couple: Crossing - The Path Unfolds remove ‘I like to think of it as a daytime painting’  
Do not otherwise alter the wording of the artwork descriptions. 
